{
  "name": "payment-processor",
  "root": "apps/payment-processor",
  "sourceRoot": "apps/payment-processor",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o dist/main main.go",
        "cwd": "apps/payment-processor"
      }
    }
  }
}
